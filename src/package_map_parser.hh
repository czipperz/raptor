#ifndef HEADER_GUARD_PACKAGE_MAP_PARSER_H
#define HEADER_GUARD_PACKAGE_MAP_PARSER_H

#include <map>
#include <string>

std::map<std::string, std::string>
package_map_parser(const char* file_name);

#endif
