#ifndef HEADER_GUARD_COPY_PKGBUILD_TO_TMP_H
#define HEADER_GUARD_COPY_PKGBUILD_TO_TMP_H

/// cd to a temporary directory, download the package specified and
/// untar it
void initialize_package(const char* package_name);

#endif
